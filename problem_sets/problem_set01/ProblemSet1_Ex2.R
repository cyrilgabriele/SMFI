# ============================================================
# Problem Set 1 — Exercise 2
# ============================================================
# ---------- Inputs ----------
S0    <- 100
mu    <- 0.08
sigma <- 0.20
T     <- 1
s     <- 250

dt    <- 1 / s
steps <- T * s
tgrid <- seq(0, T, by = dt)   # length = steps + 1
